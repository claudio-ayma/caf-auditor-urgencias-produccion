# ===================================================================
# CONFIGURACION DEL SISTEMA DE AUDITORIA DE URGENCIAS
# ===================================================================
# IMPORTANTE: Copiar este archivo a .env y completar con valores reales
# El archivo .env esta en .gitignore y NO se versionara
# ===================================================================

# -------------------------------------------------------------------
# OPENROUTER - ACCESO A CLAUDE (IA)
# -------------------------------------------------------------------
# Obtener API key en: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Modelos de IA
DEFAULT_MODEL=anthropic/claude-sonnet-4.5
FALLBACK_MODEL=anthropic/claude-sonnet-4

# -------------------------------------------------------------------
# MYSQL - BASE DE DATOS PRODUCCION
# -------------------------------------------------------------------
# Configuracion de conexion a la base de datos del hospital
MYSQL_HOST=192.168.30.16
MYSQL_PORT=3306
MYSQL_USER=tu_usuario
MYSQL_PASSWORD=tu_password_seguro
MYSQL_DATABASE=foianini.prod

# -------------------------------------------------------------------
# MINIO - ALMACENAMIENTO DE ARCHIVOS
# -------------------------------------------------------------------
# Endpoint de MinIO (sin http:// o https://)
# Ejemplos:
#   - Localhost: localhost:9000
#   - Servidor remoto: minio.ejemplo.com:9000
#   - Con puerto personalizado: 192.168.1.100:9090
MINIO_ENDPOINT=192.168.30.62:9000

# Credenciales de acceso a MinIO
# Estas son como "usuario" y "contrasena" para MinIO
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=tu_secret_key

# Nombre del bucket donde se guardaran los archivos de URGENCIAS
# El bucket se creara automaticamente si no existe
# IMPORTANTE: Usar un bucket DIFERENTE al de emergencias
MINIO_BUCKET_NAME=auditoria-urgencias

# Usar SSL/TLS (true o false)
# true: Conexion segura HTTPS (requiere certificados)
# false: Conexion HTTP sin cifrar (desarrollo local)
MINIO_USE_SSL=false

# -------------------------------------------------------------------
# SMTP - ENVIO DE CORREOS
# -------------------------------------------------------------------
# Configuracion del servidor SMTP para envio automatico de reportes

# Servidor y puerto SMTP
SMTP_SERVER=mail.correo-caf.com
SMTP_PORT=465

# Credenciales de autenticacion
SMTP_USER=no.responder@correo-caf.com
SMTP_PASSWORD=tu_password_smtp

# Remitente (opcional, si no se especifica usa SMTP_USER)
SMTP_FROM_EMAIL=no.responder@correo-caf.com
SMTP_FROM_NAME=Sistema de Auditoria CAF - Urgencias

# Destinatarios (separados por coma)
# Ejemplo: usuario1@correo.com, usuario2@correo.com, usuario3@correo.com
EMAIL_DESTINATARIOS=correo1@ejemplo.com,correo2@ejemplo.com

# ===================================================================
# NOTAS IMPORTANTES
# ===================================================================
#
# 1. SEGURIDAD:
#    - NUNCA versionar el archivo .env en git
#    - Usar credenciales de solo lectura para MySQL si es posible
#    - Cambiar credenciales por defecto de MinIO (minioadmin)
#
# 2. MINIO - CONFIGURACION RAPIDA:
#    Para desarrollo local con Docker:
#      docker run -p 9000:9000 -p 9001:9001 \
#        -e MINIO_ROOT_USER=minioadmin \
#        -e MINIO_ROOT_PASSWORD=minioadmin \
#        minio/minio server /data --console-address ":9001"
#
#    Acceder a la consola web: http://localhost:9001
#
# 3. MYSQL:
#    - El sistema requiere acceso de LECTURA a las tablas de evoluciones
#    - No requiere permisos de escritura en la base de datos
#    - Se recomienda crear un usuario de solo lectura
#
# 4. OPENROUTER:
#    - Se factura por tokens consumidos
#    - Claude Sonnet 4.5: ~$3/1M input tokens, ~$15/1M output tokens
#    - Costo estimado: ~$1.20/dia para 60 atenciones
#    - Verificar saldo en: https://openrouter.ai/credits
#
# 5. DIFERENCIA CON EMERGENCIAS:
#    - Este sistema audita URGENCIAS (casos de menor complejidad)
#    - Usa un bucket separado en MinIO: auditoria-urgencias
#    - Los correos indican claramente que son de URGENCIAS
#
# ===================================================================
